name: Test

on:
  push:
    branches:
      - main
    pull_request:

jobs:
  test:
    name: Test
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install
        run: bash ./etc/scripts/install.sh
